# i18n

## 支援言語

- ja (日本語)
- en (English)

## 辞書

`src/lib/i18n/dictionaries.ts` に静的辞書を定義。

## 使用方法

```tsx
import { getDictionary } from "@/lib/i18n/dictionaries";

const dict = getDictionary("ja");

return <p>{dict.common.loading}</p>;
```

## 拡張

新言語を追加する場合は dictionaries に言語キーを追加。
